# Plot Conclusions

## optimal_time_by_level.png
No hubo corridas suficientes para resumir esta figura.

## optimal_frontier_count_by_level.png
No hubo corridas suficientes para resumir esta figura.

## optimal_nodes_expanded_by_level.png
No hubo corridas suficientes para resumir esta figura.

## non_optimal_time_by_level.png
Dentro de algoritmos no optimos, Greedy (combined, allow) obtuvo el menor tiempo promedio, mientras que DFS (prune) quedo con los valores mas altos en el conjunto analizado.

## non_optimal_frontier_count_by_level.png
Dentro de algoritmos no optimos, Greedy (combined, allow) obtuvo el menor nodos frontera promedio, mientras que DFS (prune) quedo con los valores mas altos en el conjunto analizado.

## non_optimal_nodes_expanded_by_level.png
Dentro de algoritmos no optimos, Greedy (combined, allow) obtuvo el menor nodos expandidos promedio, mientras que DFS (prune) quedo con los valores mas altos en el conjunto analizado.

## bfs_vs_dfs_time_errorbars.png
DFS fue la familia con mejor promedio de tiempo en esta comparacion, y DFS (prune) quedo como la variante mas fuerte.

## bfs_vs_dfs_nodes_expanded_errorbars.png
DFS fue la familia con mejor promedio de nodos expandidos en esta comparacion, y DFS (prune) quedo como la variante mas fuerte.

## greedy_vs_astar_time_errorbars.png
Greedy fue la familia con mejor promedio de tiempo en esta comparacion, y Greedy (combined, allow) quedo como la variante mas fuerte.

## greedy_vs_astar_nodes_expanded_errorbars.png
Greedy fue la familia con mejor promedio de nodos expandidos en esta comparacion, y Greedy (combined, allow) quedo como la variante mas fuerte.

## cost_by_level_errorbars.png
No hubo corridas suficientes para resumir esta figura.

## boxplot_time.png
La mayor dispersion de tiempo aparecio en Greedy (static_deadlock, allow), por eso este box plot complementa bien a los promedios con barras de error.

## boxplot_frontier_count.png
La mayor dispersion de nodos frontera aparecio en DFS (prune), por eso este box plot complementa bien a los promedios con barras de error.

## boxplot_nodes_expanded.png
La mayor dispersion de nodos expandidos aparecio en DFS (prune), por eso este box plot complementa bien a los promedios con barras de error.

## boxplot_cost.png
La mayor dispersion de costo aparecio en DFS (prune), por eso este box plot complementa bien a los promedios con barras de error.
