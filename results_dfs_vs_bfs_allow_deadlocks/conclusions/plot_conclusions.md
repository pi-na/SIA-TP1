# Plot Conclusions

## optimal_time_by_level.png
Dentro de algoritmos optimos, BFS (allow) obtuvo el menor tiempo promedio, mientras que BFS (allow) quedo con los valores mas altos en el conjunto analizado.

## optimal_frontier_count_by_level.png
Dentro de algoritmos optimos, BFS (allow) obtuvo el menor nodos frontera promedio, mientras que BFS (allow) quedo con los valores mas altos en el conjunto analizado.

## optimal_nodes_expanded_by_level.png
Dentro de algoritmos optimos, BFS (allow) obtuvo el menor nodos expandidos promedio, mientras que BFS (allow) quedo con los valores mas altos en el conjunto analizado.

## non_optimal_time_by_level.png
Dentro de algoritmos no optimos, DFS (allow) obtuvo el menor tiempo promedio, mientras que DFS (allow) quedo con los valores mas altos en el conjunto analizado.

## non_optimal_frontier_count_by_level.png
Dentro de algoritmos no optimos, DFS (allow) obtuvo el menor nodos frontera promedio, mientras que DFS (allow) quedo con los valores mas altos en el conjunto analizado.

## non_optimal_nodes_expanded_by_level.png
Dentro de algoritmos no optimos, DFS (allow) obtuvo el menor nodos expandidos promedio, mientras que DFS (allow) quedo con los valores mas altos en el conjunto analizado.

## bfs_vs_dfs_time_errorbars.png
BFS fue la familia con mejor promedio de tiempo en esta comparacion, y BFS (allow) quedo como la variante mas fuerte.

## bfs_vs_dfs_nodes_expanded_errorbars.png
BFS fue la familia con mejor promedio de nodos expandidos en esta comparacion, y BFS (allow) quedo como la variante mas fuerte.

## greedy_vs_astar_time_errorbars.png
No hubo corridas suficientes para resumir esta figura.

## greedy_vs_astar_nodes_expanded_errorbars.png
No hubo corridas suficientes para resumir esta figura.

## cost_by_level_errorbars.png
Los algoritmos optimos mantuvieron un costo medio menor o igual en 4/4 niveles frente a los no optimos.

## boxplot_time.png
La mayor dispersion de tiempo aparecio en BFS (allow), por eso este box plot complementa bien a los promedios con barras de error.

## boxplot_frontier_count.png
La mayor dispersion de nodos frontera aparecio en BFS (allow), por eso este box plot complementa bien a los promedios con barras de error.

## boxplot_nodes_expanded.png
La mayor dispersion de nodos expandidos aparecio en BFS (allow), por eso este box plot complementa bien a los promedios con barras de error.

## boxplot_cost.png
La mayor dispersion de costo aparecio en DFS (allow), por eso este box plot complementa bien a los promedios con barras de error.
